import { Stack, Typography } from "@mui/material";

const RecruiterDashboardRoute = () => {
  return (
    <Stack>
      <Typography variant="h4">Dashboard</Typography>
    </Stack>
  );
};

export default RecruiterDashboardRoute;
